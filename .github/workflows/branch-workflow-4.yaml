name: branch workflow 4

on:
  workflow_dispatch:
    inputs:
      dataNotJson:
        type: string
        required: true
      testValue:
        type: string
        required: true
  repository_dispatch:
    types: [pull-request-event]

env:
  AWS_REGION: "eu-west-2"

permissions:
  id-token: write
  contents: read

jobs:
  runTerraform:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v4

      - name: Run stuff
        run: echo "this is working for me"

      - name: Generate .tfvars File
        run: |

          data="${{ github.event.inputs.dataNotJson }}"
          echo "-----some data-----"
          echo $data
          echo "-----some data-----"
          echo "-----testValue-----"
          echo "${{ github.event.inputs.testValue }}"
          echo "-----testValue-----"
